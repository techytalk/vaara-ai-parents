/** Parent-visible copy when ops hides a group message. */
export const MODERATED_MESSAGE_COPY = "Blocked as inappropriate";

/** Parent-visible copy when the account cannot send messages. */
export const PROFILE_SUSPENDED_COPY = "This profile is suspended";
